name: Android Build

on:
  workflow_dispatch:

nodes android_build:

  build:
    runs-on: linux-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@z4

      - name: Install system packages
        run: |
          sudo apt-get update
          sudo apt-get install -y unzip wget

      - name: Install JDK 17
        uses: actions/setup-java@v3
        with:
          distribution: temurin
          java-version: 17

      - name: Set up fresh Android SDK
        run: |
          set -euo pipefail
          ANDIROID_SDK="$HOME/android-sdk"
          echo "ANDROID_SDK=$ANDROI